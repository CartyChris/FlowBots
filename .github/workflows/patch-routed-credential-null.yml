name: Patch routed credential null

on:
  push:
    branches: [feature/rakazo-local-ai-os]
    paths:
      - ".github/workflows/patch-routed-credential-null.yml"
      - "scripts/patch-routed-credential-null.mjs"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feature/rakazo-local-ai-os
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply exact-once patch
        run: node scripts/patch-routed-credential-null.mjs
      - name: Verify minimal diff
        run: |
          set -euo pipefail
          git diff --check
          changed="$(git diff --name-only)"
          test "$changed" = "packages/adapters/src/executor.ts"
          git diff -- packages/adapters/src/executor.ts
      - name: Commit patch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/adapters/src/executor.ts
          git commit -m "fix(adapters): normalize routed credential absence"
          git push origin HEAD:feature/rakazo-local-ai-os
